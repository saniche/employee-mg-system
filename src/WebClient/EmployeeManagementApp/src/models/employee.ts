//interface employee with properties (firstname, lastname, position, salary,phoneNumber, email)
export interface Employee {
  id: number;
  firstName: string;
  lastName: string;
  position: string;
  salary: number;
  phoneNumber: string;
  email: string;
}